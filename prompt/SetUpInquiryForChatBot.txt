Objective #1:

    1). I wanted to implement a chatbot for my Inquiry system.

    2). For the chatbot, I wanted to use the Botman chatbot since it's the easiest library to choose for chatbot.

    3). Create a table for chatbot that the user can set and create an content for chatbot. The table creation must use the
        artisan migration. Create a table needed for the requirements.

    4). Create a model for newly created tables using the artisan migration and the other file e.g Controller, Model, Request
        and etc must use artisan command.

    5). The process is where the studio owner will setup the chatbot contents that the customer or client can ask questions or
        inquiries and the chatbot will answer their questions.

    6). The chatbot scope only is based on the owner setup only.

    7). The conversation between client/customer and chatbot will also stored in the database tables.

    8). Give the summary of the propose changes and modification before providing all the needed code.
        Wait for the "Go!" command and execute.

Objective #2:

    1). Now here in the client side, in the booking details, I added the Chat with Bot Agent button in the Contact Information
        section where the user or client chat with bot.

    2). When pressed, it shows the modal and they start talking.
    
    3). For now focus for Studio Bot Agent, and the freelancer should has the same feature but not now (Just focus on studio side).


Objective #3:
    
    1). Can u make the switch for status into a dropdown instead of using the switch.

    2). Manage Intents Tab:

        - make the badge use the class name "badge badge-soft-(color)"
        - In the action buttons of the table, the eye icon should be a view, when clicked it shows the modal of the details.
        - In that modal, It will display the details of the Intents. The modal layout should follow this template I made.
        - Now the edit icon should show the modal where I can edit and change anything in the intents. Make the edit functionalities work

    3). Currently when I click the edit button, it shows the error on response:
        Call to undefined method App\Http\Controllers\StudioOwner\ChatbotConfigController::getIntent()
    
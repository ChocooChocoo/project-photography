Objective #1: Role and Permissions Creations

  1). Create a table using artisan migration for both "tbl_roles" and "tbl_permissions"

  2). Create a models for newly created tables  using artisan migration as well (already created just update the code).

  3). In the creation of the roles, this is where my RBAC starts. The fields must have these columns:

      | Column        |
      | ------------- |
      | `id`          |
      | `name`        |
      | `description` |
      | `status`      |
      | `created_at`  |
      | `updated_at`  |

  4). In the creation of Roles in the field must be select dropdown with these roles. Also in the "tbl_users" it stores
      the (role) and (user_type) based on the selected role

      Human Resouce Manager     has a role = studio-hr         has a user_type = manager
      Human Resouce Staff       has a role = studio-hr         has a user_type = staff
      Finance Manager           has a role = studio-finance    has a user_type = manager
      Finance Staff             has a role = studio-finance    has a user_type = staff

  5). Now in the permission tables, this is where the permissions of every roles has. The table must have these columns:

      | Column          |
      | --------------- |
      | `id`            |
      | `name`          |
      | `description`   |
      | `status`        |
      | `created_at`    |
      | `updated_at`    |
  
  6). Now for the linking references, create a table name "tbl_role_permissions" using migration and must have columns of:

      | Column                       |
      |------------------------------|
      | id | role_id | permission_id |

  7). For the user and roles refrences, create a table name "tbl_user_roles" using migration and must have columns of:

      | Column       |
      |--------------|
      | id | role_id |

  8). Before you execute and provide the changes, give the summary of the process first and wait for the Go command might as well
      before we proceed to the Objective #2 after we finished the Objective #1.

Objective #2: Employee Creation along with their Role/Permissions

  1). Here in the current employee creation, I noticed that my RBAC process is wrong and I can call it EBAC lol. Now
      since we already works with roles and permission, I wanted to remove the CRUD permission and replace it with Role-Based,
      from the word it self RBAC.

  2). In the employee creation, Studio Position Section, it has  Employee's Role and Role Type. Now this is where the change will
      happen. In the Employees Role, it would reflected to the created roles.

  3). In the creation of Roles in the field must be select dropdown with these roles. Also in the "tbl_users" it stores
      the (role) and (user_type) based on the selected role

    Human Resouce Manager     has a role = studio-hr         has a user_type = manager
    Human Resouce Staff       has a role = studio-hr         has a user_type = staff
    Finance Manager           has a role = studio-finance    has a user_type = manager
    Finance Staff             has a role = studio-finance    has a user_type = staff


Objective #4: Fix the errors and missing important things

    1). I noticed that in the creation, the adding of the photographer field disappeared.
        Do not remove the adding of the photographer
    
    2). In the view, it still only shows the photograpger roles and the other created employees is not showing. For Example
        I register an employee as studio hr and it stores in database but it didnt shows or display in the table.